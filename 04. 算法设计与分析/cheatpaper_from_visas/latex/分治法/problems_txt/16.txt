大题15：两元素和为X的分治算法
1. 题目重现
给定由个实数构成的集合和实数，判断中是否存在两个元素的和为，要求设计分治算法求解，并分析时间复杂度。
2. 考点定位
分治法的应用、归并排序的结合、双指针查找策略。
3. 解题思路拆解
结合分治“分解→求解→合并验证”步骤：
分解：先用归并排序对排序（保证复杂度），再将有序划分为左子集和右子集；
求解：递归判断、内是否存在两元素和为；
合并验证：用双指针遍历和，判断是否存在跨子集的两元素和为。
4. 详细步骤推导
步骤1：预处理排序
用归并排序对排序，得到有序数组，时间复杂度。
步骤2：分治求解
定义函数：
若：返回；
令，，；
若或：返回；
双指针验证跨子集情况：
初始化（起始）、（末尾）；
循环当且：
；
若：返回；
若：（需更大的和）；
若：（需更小的和）；
循环结束后返回。
步骤3：调用函数
最终结果为。
5. 答案呈现
算法步骤：
归并排序得到有序数组；
分治划分为左右子集，递归判断子集内是否有解；
双指针遍历左右子集，验证跨子集是否有解。
时间复杂度：排序为，分治递归方程为，由Master定理得整体复杂度为。
6. 改编思路拓展
改编方向1：找三个元素的和为
题目调整：判断中是否存在三个元素的和为；
解题思路调整：排序后固定每个元素，调用原算法判断中是否存在两元素和为（跳过本身），整体复杂度为。
改编方向2：找和最接近的两个元素
题目调整：找到中两元素和最接近的一对；
解题思路调整：排序后分治，递归找左右子集的最接近对，再找跨子集的最接近对，双指针遍历跨子集时记录最小的，复杂度仍为。