大题2：折半搜索（PPT例：搜索40）
1. 题目重现
给定有序数组（索引0-5），使用折半搜索算法查找目标元素40，要求展示每次查找的、、值及比较过程，若查找成功返回元素下标，失败返回-1。
2. 考点定位
分治法-折半搜索（核心考查有序数组的分治查找策略、边界调整逻辑、终止条件判断）。
3. 解题思路拆解
初始化边界：（数组起始索引），（数组结束索引，）；
计算中间索引：；
比较目标值与：
若相等：查找成功，返回；
若目标值> ：目标元素在右半区间，调整；
若目标值< ：目标元素在左半区间，调整；
终止条件：时，查找失败，返回-1。
4. 详细步骤推导
初始化：，，目标值；
第1次查找：
，；
比较：，目标在右半区间，调整；
第2次查找：
，；
比较：，目标在左半区间，调整；
第3次查找：
，；
比较：，查找成功，返回。
5. 答案呈现
目标元素40的下标为：
6. 改编思路拓展
改编方向1：查找目标元素的插入位置（修改问题需求）
题目调整：给定有序数组，查找目标元素35的插入位置（插入后数组仍有序）；
解题思路调整：按原折半搜索步骤，当时，即为插入位置（如35的查找过程：最终为3，插入后数组为）。
改编方向2：旋转有序数组的折半搜索（修改数组结构）
题目调整：给定旋转有序数组（原有序数组旋转后的结果），查找目标元素10；
解题思路调整：先判断所在的子区间是否有序（如时，，左区间有序），再根据目标值与有序区间边界的关系缩小查找范围，时间复杂度仍为。