% ========== 自动生成的目录内容 ==========
\vspace{8pt}
1. \textbf{0/1背包问题}\hfill 2\\
给定n个物品和容量为C的背包，物品i的重量为$w_i$，价值为$v_i$。每个物品只能选择"装入"或"不装入"（不可分割），要求在背包总重量不超过C的前提下，使装入物品的总价值最大。

\vspace{8pt}
2. \textbf{数字加减乘号最大结果}\hfill 2\\
给定N个1-9的数字$v_1, v_2, \ldots, v_n$，不改变数字相对位置，在相邻数字间加入K个乘号和$N-K-1$个加号（每位置必加一个符号），括号可任意添加，使最终结果最大。

\vspace{8pt}
3. \textbf{序列划分最小化最大值和}\hfill 3\\
给定长度为N的整数序列$(a_1, a_2, \ldots, a_n)$，将其划分为若干连续子序列，每个子序列的和$\le B$。求使所有子序列"最大值"之和最小的划分方案。

\vspace{8pt}
4. \textbf{树的节点着色（最大化黑色节点数）}\hfill 3\\
对一棵以r为根的树进行节点着色，每个节点可着黑色或白色。约束：相邻节点不能着相同黑色（但可同白）。求使树中黑色节点数最多的着色方案。

\vspace{8pt}
5. \textbf{字符串分词（最大化质量和）}\hfill 4\\
给定字符串$y = y_1y_2\ldots y_n$，将其切分为若干连续子串（每个子串为合法词汇）。函数$quality(x)$返回词汇$x$的质量，求使总质量最大的分词方案。

\vspace{8pt}
6. \textbf{带权活动选择问题}\hfill 4\\
给定$n$个活动，每个活动$a_i = (s_i, f_i, v_i)$，其中$s_i$为开始时间，$f_i$为结束时间，$v_i$为权重。选择若干不重叠的活动，使权重和最大。

\vspace{8pt}
7. \textbf{最大子数组问题（分治法）}\hfill 5\\
给定$n$个整数（有正有负）的数组$A$，设计$O(n \log n)$算法，找出和最大的非空连续子数组。

\vspace{8pt}
8. \textbf{最长非降子序列（LIS）}\hfill 5\\
给定长度为$n$的序列$A$，求最长非降子序列（LIS）的长度（非降指$a_i \le a_j$对$i < j$成立，子序列不要求连续）。

\vspace{8pt}
9. \textbf{矩阵连乘问题}\hfill 6\\
给定$n$个矩阵$M_1, M_2, \ldots, M_n$，其中$M_i$维数为$r_{i-1} \times r_i$。矩阵乘法满足结合律，不同的括号化方式导致不同的乘法次数。求使乘法运算次数最少的最优括号化方案。

\vspace{8pt}
10. \textbf{钢条切割问题}\hfill 6\\
给定长度为$n$英寸的钢条和价格表$p_i$（$i=1 \sim n$），钢条可切割为任意段，求切割后总收益最大。例如：$n=4$，$p=[1,5,8,9]$时，最优方案为切成2段长度2，收益$5+5=10$。

\columnbreak
\vspace{8pt}
11. \textbf{最长公共子序列（LCS）}\hfill 7\\
给定两个序列$X=(x_1,x_2,\ldots,x_m)$和$Y=(y_1,y_2,\ldots,y_n)$，求其最长公共子序列的长度及具体序列（子序列不要求连续）。例如：$X=\text{"ABCBDAB"}$，$Y=\text{"BDCABA"}$，LCS为$\text{"BCAB"}$或$\text{"BDAB"}$，长度为4。

\vspace{8pt}
12. \textbf{最优二叉搜索树}\hfill 7\\
给定$n$个有序关键字$k_1 < k_2 < \ldots < k_n$，每个关键字$k_i$的成功搜索概率为$p_i$，失败搜索概率为$q_0,q_1,\ldots,q_n$。构造使平均搜索长度最小的二叉搜索树。

\vspace{8pt}
13. \textbf{DAG最安全路径问题}\hfill 8\\
给定有向无环图$G=(V,E)$，边$e_i$的事故概率为$p_i$。路径安全性定义为路径上所有边概率的乘积。求从起点$s$到终点$t$的最安全路径（安全性最大的路径）。

\vspace{8pt}
14. \textbf{环形房间化学品存放问题}\hfill 8\\
给定$n$个首尾相连的环形房间，每个房间容量为$w_i$（杂物房间容量为0）。约束：化学品不能存放在相邻房间。求最多能存放的化学品总容量。

\vspace{8pt}
15. \textbf{受限最短路径（带钱数约束）}\hfill 9\\
给定无向图$G=(V,E)$，边$e_{uv}$长度为$l_{uv}$，顶点$u$花费为$c[u]$。从$s$出发到$t$，初始钱数为$M$。每经过顶点$u$需支付$c[u]$（剩余钱数$\ge c[u]$才能通过），求$s$到$t$的最短路径长度。

\vspace{8pt}
16. \textbf{完全占满背包（恰好装满）}\hfill 9\\
给定$n$个物品，物品$i$的大小为$w_i$、价值为$v_i$，背包容量为$C$。要求选择一组物品，\textbf{恰好占满}背包容量（选中物品大小和恰好为$C$），且总价值最大。

\vspace{8pt}
17. \textbf{数组最大子段的个数（O(n)）}\hfill 10\\
设$A[1..n]$是长度为$n$的数组，存放可重复的整数。最大子段指其和不小于任何子段和的子段。要求设计$O(n)$算法，求最大子段的个数。

\vspace{8pt}
18. \textbf{最少插入字符使字符串成为回文}\hfill 10\\
给定字符串$S$，设计算法在$S$中插入最少的字符，使其成为回文字符串。例如：$S=\text{"ab"}$，插入1个字符得"aba"或"bab"。

\vspace{8pt}
19. \textbf{二维矩阵的最大子矩阵和}\hfill 11\\
给定$m \times n$的二维整数矩阵$A$（含正、负、零），找到和最大的子矩阵，并输出其和。
