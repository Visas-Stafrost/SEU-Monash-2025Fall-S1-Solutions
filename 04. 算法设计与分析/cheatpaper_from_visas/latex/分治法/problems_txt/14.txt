大题17：数组中两元素和大于0的对数量
1. 题目重现
给定大小为的数组，求数组中不同对（）的数量，使得。例如，数组的符合条件的对为、，共2个。
2. 考点定位
分治法的应用（结合归并排序）、双指针统计策略、时间复杂度优化（从暴力到）。
3. 解题思路拆解
遵循分治“排序→分解→递归统计→合并统计”步骤：
排序：用归并排序对数组升序排列（为双指针高效统计做准备）；
分解：将排序后的数组划分为左子集和右子集；
递归统计：分别计算内部、内部符合条件的对数量；
合并统计：用双指针遍历和，统计跨子集的符合条件的对数量；
总数量为“左内部+右内部+跨子集”三者之和。
4. 详细步骤推导
步骤1：预处理排序
用归并排序对数组升序排列，时间复杂度。例如数组排序后为。
步骤2：分治统计（定义函数）
若数组长度：返回0（无法形成对，递归终止条件）；
令，，；
递归计算：（内部的符合对数量），（内部的符合对数量）；
合并统计跨子集的对数量：
初始化（的起始，对应中最小元素）、（的末尾，对应中最大元素），；
循环当且：
若：
因是升序，，故中从到末尾的所有元素与的和都>0；
；
（尝试中更小的元素）；
否则（）：
（尝试中更大的元素）。
示例验证（数组）
分解：，；
计算：递归分解为和，内部对数量均为0；合并统计的跨对：，故；
计算：长度为1，返回0；
计算：时，，；
总数量：，与示例一致。
5. 答案呈现
算法步骤：
归并排序数组为升序；
分治划分为左右子集，递归统计子集内的符合对数量；
双指针统计跨子集的符合对数量；
总数量为“左内部+右内部+跨子集”之和。
时间复杂度：排序为，分治递归方程为，整体复杂度为。
6. 改编思路拓展
改编方向1：统计两元素和小于0的对数量
题目调整：求数组中的不同对数量；
解题思路调整：排序后双指针从起始、起始遍历，若，则与中从到末尾的元素和都<0，，；否则。
改编方向2：统计三元组和大于0的数量
题目调整：求数组中不同三元组（）的数量，使得；
解题思路调整：排序后固定每个元素，调用原算法统计前个元素中两元素和的对数量，累加所有的结果，复杂度为。
要不要我帮你整理一份分治法+双指针统计类题目的核心步骤模板，方便你考试时快速套用？
|（注：文档部分内容可能由 AI 生成)
天际轮廓问题：分治算法求解