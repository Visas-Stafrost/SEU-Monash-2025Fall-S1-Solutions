大题3：折半搜索（PPT例：搜索25）
1. 题目重现
给定有序数组（索引0-5），使用折半搜索算法查找目标元素25，要求展示每次查找的、、值及比较过程，若查找成功返回元素下标，失败返回-1。
2. 考点定位
分治法-折半搜索（核心考查查找失败时的边界调整逻辑、终止条件判断）。
3. 解题思路拆解
与“搜索40”一致，遵循折半搜索的通用步骤，重点关注目标元素不存在时，与的动态调整过程，直至触发失败条件。
4. 详细步骤推导
初始化：，，目标值；
第1次查找：
，；
比较：，目标在左半区间，调整；
第2次查找：
，；
比较：，目标在右半区间，调整；
第3次查找：
，；
比较：，目标在右半区间，调整；
终止条件：此时，，，查找失败，返回-1。
5. 答案呈现
目标元素25不存在，返回：
6. 改编思路拓展
改编方向1：查找有序数组中第一个大于目标元素的元素（修改问题需求）
题目调整：给定有序数组，查找第一个大于25的元素（答案为30，下标2）；
解题思路调整：在折半搜索中，若，则调整；若，则记录当前并调整，最终即为目标元素的下标。
改编方向2：允许重复元素的有序数组查找目标元素的左边界（修改数据结构）
题目调整：给定有序数组，查找目标元素20的左边界（下标1）；
解题思路调整：当时，不立即返回，而是调整，继续向左查找，最终即为左边界下标（若则存在，否则不存在）。